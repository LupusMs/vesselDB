<!DOCTYPE html>
<%#include "vessel.h" %>
<% tfetch(QVariantMap, vessel); %>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= controller()->name() + ": " + controller()->activeAction() %></title>
</head>
<body>
<p style="color: red"><%=$ error %></p>
<p style="color: green"><%=$ notice %></p>

<h1>Editing Vessel</h1>

<%== formTag(urla("save", vessel["imo"]), Tf::Post) %>
  <p>
    <label>Imo<br /><input type="text" name="vessel[imo]" value="<%= vessel["imo"] %>" readonly="readonly" /></label>
  </p>
  <p>
    <label>Name<br /><input type="text" name="vessel[name]" value="<%= vessel["name"] %>" /></label>
  </p>
  <p>
    <label>Build Year<br /><input type="text" name="vessel[buildYear]" value="<%= vessel["buildYear"] %>" /></label>
  </p>
  <p>
    <label>Type<br /><input type="text" name="vessel[type]" value="<%= vessel["type"] %>" /></label>
  </p>
  <p>
    <input type="submit" value="Save" />
  </p>
</form>

<%== linkTo("Show", urla("show", vessel["imo"])) %> |
<%== linkTo("Back", urla("index")) %>
</body>
</html>
